# Critical containers to monitor (one per line)
# Lines starting with # are comments, blank lines are ignored
# Edit this file to add/remove monitored services
# Install: scp to homeserver:/opt/homeserver/configs/monitoring/critical-containers.conf
caddy
pihole
immich-server
immich-postgres
jellyfin
